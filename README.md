# World-Cup-Database
A PostgreSQL + Bash project that builds a full World Cup results database, loads data from CSV automatically, and runs analytical SQL queries. Includes schema design, data ingestion scripts, and a complete database export.
